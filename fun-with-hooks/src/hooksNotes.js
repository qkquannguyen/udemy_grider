/**
 * useState()       --> Allow a FC to use component-level state
 * useEffect()      --> Allow FC to use 'lifecycle methods'
 * useContext()     --> Allow a functional component to use the context system
 * useRef()         --> Allow a FC to use the reff system
 * useReducer()     --> Allow a FC to store data through a 'reducer'
 */